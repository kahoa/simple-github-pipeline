# Node.js Calculator Project

This is a simple Node.js project that includes a basic calculator with tests. The project also includes a GitHub Actions pipeline for continuous integration (CI) that runs tests automatically on each push to the repository.
